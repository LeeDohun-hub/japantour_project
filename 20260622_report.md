# 2026-06-22 業務報告

## 1. 業務内容

### 教保文庫の誤マッチ問題（青渓川・光化門）重複バグ
・フロントの앵커から教保文庫を強制除外、プランの venue とURL先の place が不一致の場合は place を破棄
・同一場所が venue URL と経路・説明行で二重マッチして2枚出る問題を解消
・昼食/夕食スロットの抜けを防止、具体的な店名のみ許可

### 日本語URL・場所名ハルシネーションの修正
・Naver検索URLにひらがな・カタカナが混入した場合、直前行の韓国語名で置換する `_fix_japanese_naver_urls` を追加
・「2日目」「観光スポット ·」「コスパ抜群」等のUIノイズを場所名として扱わないよう除外

---

## 2. 学んだこと・気づいたこと

・Naver `search_local` はレビュー量順ソートのため、ランドマーク検索でも高リビューの隣接店が先頭に来る → 名前一致ガードが必須
・URLが違っても同一場所なら韓国語表示名で正規化すれば重複判定できる（`_sameStop` を補完）
・LLMはゼロ候補フォールバック時に日本語名をそのままURLへ挿入する傾向があり、生成側プロンプト＋後処理の二段構えが有効
・プランテキストの venue を source of truth とし、API place が不一致なら捨てる方が地図と詳細プランの整合が取れる

---

## 3. 次の目標

・PPT練習
