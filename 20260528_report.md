# 2026-05-28 업무 보고

## 1. 업무내용

### 여행 목적지/지역 선택 로직 개선

- 관광 목적지는 복수 선택이 아니라 한 개만 선택 가능하도록 정책 방향을 확정했다.
- 광역 에리어만 선택했을 때 AI가 무작위로 아무 지역을 고르는 방식은 위험하므로, 관광 가치가 있는 후보군 안에서 조건 기반 추천을 하는 방향으로 정리했다.

### 플랜 생성 품질 개선

- 관광 후보 필터를 강화했다.
- Google Places에서 관광지가 아닌 일반 기관이 관광 후보로 들어오는 문제를 수정했다.
  - `대한노인회 강릉시지부` 같은 협회/기관/사무소 계열 장소를 제외하도록 처리했다.
  - `관광정보센터`, `관광정보센타`, `観光情報センター`, `Tourist Information Center`, `Visitor Center` 계열도 관광 목적지 후보에서 제외했다.
- 관광 후보는 `tourist_attraction`, `museum`, `park`, `shopping_mall`, `historical_landmark` 등 실제 관광 성격이 있는 타입 위주로 통과시키도록 했다.
- 지역 밖 장소가 섞이는 문제를 줄이기 위해 Places 검색 결과의 실제 주소와 선택 지역을 더 엄격히 비교하도록 했다.
- 한국관광공사 API들을 플랜 품질 보강용으로 활용하는 방향을 정리하고 구현했다.

### 일정/지도 UI 개선

- Day 탭이 실제 지도 마커가 있는 날만 표시되는 문제를 수정했다.
- Day 배열은 항공편에서 계산된 전체 여행 기간을 기준으로 맞추도록 개선했다.
- 첫날/마지막날 공항과 숙소 이동, 숙소 출발 기준 동선을 지도에 더 안정적으로 반영하는 방향으로 보강했다.
- 동일 장소가 여러 날 반복되더라도 지도/카드 표시가 불완전하게 빠지지 않도록 관련 로직을 조정했다.
- 일본인 유저 대상 서비스이므로 UI 표시는 일본어 중심으로 유지하는 방향을 확정했다.
- 항공, 숙소, 입국 준비, 교통카드/WOWPASS, 통신/eSIM, 결제/환전 등을 사용자 입력에 따라 자동 생성하도록 했다.
- 공유 링크/PDF 저장 기능 추가 

## 2. 배운것이해한것

- 여행 서비스에서 “랜덤 추천”은 랜덤이면 위험하다. 관광 가치가 있는 후보군, 이동 가능성, 유저 취향, 여행 기간을 반영한 조건부 랜덤이 필요하다.
- 광역 지역과 시군구가 섞이는 서비스에서는 텍스트 이름만 믿으면 위험하다. 특히 `광주`, `고성`, `중구`, `동구`처럼 동명이인 지역은 내부 ID를 source of truth로 삼아야 한다.
- 협회, 사무소, 복지관, 정보센터처럼 여행 목적지로 부적합한 장소를 필터링하는 별도 품질 게이트가 필요하다.
- 공공데이터/KTO API는 직접 플랜 문장에 노출하는 것보다, 후보 선택과 검증용 참고 데이터로 쓰는 것이 서비스 품질에 더 자연스럽다.

## 3. 내일 목표

- 공유 페이지를 브라우저에서 직접 열어 실제 표시와 PDF 인쇄 결과를 확인한다.
- 여행 체크리스트 UI가 모바일/데스크톱에서 자연스럽게 보이는지 점검한다.
- 공유 페이지에도 체크리스트와 주요 장소 카드 일부를 포함할지 검토한다.
- 일정 잠금 기능 설계를 시작한다.
  - 장소 카드별 `固定` 버튼
  - 고정된 장소를 `locked_items`로 저장
  - 재생성 시 LLM에 고정 항목을 유지하도록 전달
- 장소 교체 기능의 MVP 범위를 정한다.
  - 식당만 교체
  - 관광지 한 칸만 교체
  - 전체 Day는 유지하고 특정 슬롯만 재추천
- 관광 후보 필터를 추가로 관찰한다.
  - 정보센터/협회/복지시설 외에도 실제로 튀어나오는 부적합 장소 유형을 계속 수집한다.
  - 정상 관광지까지 과하게 막히지 않는지 확인한다.
- 지역 ID 매핑을 전국 시군구 후보 전체로 더 촘촘히 확장할지 검토한다.
### 인천공항 교통 데이터 활용

- 인천국제공항공사 버스 정보와 택시 출차 정보를 일정 생성 컨텍스트에 반영하도록 했다.
- 도착/출국 공항이 ICN일 때 공항버스, 택시 대기 정보 등을 참고할 수 있게 했다.