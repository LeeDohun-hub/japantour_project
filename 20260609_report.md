# 業務日誌 — 2026-06-09

## 1. 業務内容

・ ハードコーディングでプランが組まれていた問題（最初の旅行推薦がAPIではなく固定値で常に同じ観光地を出力していた）をすべて削除し、韓国観光公社APIの呼び出しに変更
（観光名所 → 韓国観光公社API、飲食店/カフェ → Naver Place）
・ 候補プール拡大 — `_MAX_FOOD_PER_AREA` 5→8、`_MAX_ATTR_PER_AREA` 3→5、VK表示数 12→20
・ システムプロンプト修正 — 「候補不足時は休憩で代替」指示を削除 → 「隣接エリア・VKで補完、スロット省略禁止」に変更
・ 축제 날짜 未登録問題 — `searchFestival2`（日付必須）に加え `areaBasedList2(contentTypeId=85)` **API並行呼び出し**を追加、日付なし項目はDuckDuckGoウェブクローリングで日付を自動補完
・ Naver検索 誤マッピング修正 — VK addr1から区(区)単位キーワードを優先抽出（ヘウンデ区→해운대など）、クエリ「수월경화 부산」→「수월경화 해운대」
・ repairファンクション バグ修正 — `food_idx` 線形カーソル削除 → `used_food: set` 集合方式に変更、Day 3以降の食事スロット空白を解消
・ 全国8道拡張 — `_VK_CITY_SIGUNGU` / `_LEGACY_AREA_CODE_HINTS` を全国約180都市に拡張
・ PPT作成

---

## 2. 学んだこと

・ ハードコーディングされた地域は常に同じ結果を出力してしまう。APIの呼び出しによるランダマイズが必要
・ AIにコードを依頼する前に、APIやDBのカラムを箱の中身を確認するように開けて確かめることで、自分のプロジェクトに必要なデータが何かを把握してから進める必要がある
・ **LLMプロンプト設計**: 逃げ道（「休憩で代替」）を与えると、LLMは常にそちらを選ぶ。指示は「どうやって埋めるか」で書くべき

---

## 3. 明日の目標

・ PPT作成
・ 他の地域もAPI（公共データ → Naver → ）ベースでランダムに出力されるかテスト
・ 地方にも地下鉄情報が呼び出されるよう対応
