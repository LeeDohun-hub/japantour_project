# 2026-06-01 업무 리포트

## 1. 업무내용

- 숙박 선택 UI에서 `예약済み`과 `友人・家族の家`를 `宿泊先決定済み` 개념으로 통합했다.
- 스포츠 일정 표시를 종목별, 날짜별로 보기 쉽게 정리했다.
- 대한민국 17개 광역자치단체 기준으로 관광 지역 선택 구조를 정리했다.
- 지도 Day 카드와 텍스트 상세 계획이 불일치하는 디버깅


## 2. 배운것 / 이해한것

- 이번 문제의 핵심은 개별 지역 데이터가 아니라 `텍스트 상세 계획`, `지도 Day 경로`, `참조 데이터` 사이의 경계가 흐려진 구조였다.
- 프롬프트만 강화하는 것으로는 부족하다. LLM 출력이 흔들릴 수 있으므로 서버 후처리와 프론트 파서 양쪽에 안전장치가 필요하다.
- 원거리 여행 일정은 숙소 주소가 아니라 관광 목적지를 기준으로 체류 흐름을 유지해야 자연스럽다.
- 지도와 텍스트 일정은 별도 결과물이 아니라 같은 일정의 두 표현이므로, 둘 중 하나라도 임의 추론을 하면 사용자 신뢰가 크게 떨어진다.

## 3. 내일 목표

- 실제 생성 플랜을 여러 광역 지역으로 다시 테스트해서 Day 카드와 텍스트 상세 계획이 1:1로 맞는지 확인한다.
- 지도 파서의 테스트 케이스를 정식 테스트 파일로 분리해 회귀 방지한다.
- AI 출력에서 `외관 사진`, `지도`, `통로`, `평점` 같은 카드 UI 문구가 본문에 남는 문제를 추가로 정리한다.
- 식당/관광 후보가 부족할 때 추상 문장으로 빠지지 않고, 검증된 후보 또는 일정 축소로 처리되는지 확인한다.
