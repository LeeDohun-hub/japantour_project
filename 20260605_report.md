# 2026-06-05 업무 보고

---

## 1. 업무 내용

### 기능 개선
- **예산 스타일 실질 반영**: `_fmt_budget_hint()` 함수 신규 작성 — 코스파/균형/프리미엄 스타일과 중시 비용(교통·숙박·식비)을 LLM 시스템 프롬프트 컨텍스트에 구체적 행동 지시로 주입
- **wizard.js 프롬프트 누락 수정**: `priority`(중시 비용) 필드가 `【予算】` 줄에 포함되지 않던 버그 수정
- **일본 카드 결제 가이드 추가**: 플랜 결과 화면 하단에 아코디언 형태로 추가 (카드 승인 OK / 카드 거절 / 현금 소진 3가지 시나리오)

### AWS EC2 배포 (첫 배포)
- EC2 인스턴스 생성 (Ubuntu 26.04, t3.micro, ap-southeast-2 시드니)
- 보안 그룹 설정 (SSH 22 / HTTP 80 / HTTPS 443)
- Python 가상환경, Gunicorn 서비스 등록 및 실행
- PostgreSQL + pgvector 설치 및 DB 마이그레이션
- Nginx 리버스 프록시 설정
- Route 53 도메인 등록 및 A 레코드 연결 (`japantourtravel.com`)
- Certbot으로 HTTPS 인증서 발급 (Let's Encrypt)
- LINE / Google OAuth Callback URL, Naver Maps 도메인 등록
- Swap 2GB 추가 (t3.micro 메모리 부족 대응)
- Nginx proxy_read_timeout 300초로 상향 (플랜 생성 스트리밍 대응)

---

## 2. 배운 것 / 알게 된 것

- **AWS EC2 배포 흐름**: 인스턴스 생성 → SSH 접속 → 코드 업로드 → 가상환경 → DB 마이그레이션 → Gunicorn 서비스 → Nginx 리버스 프록시 → 도메인 + HTTPS 순서

- **Nginx란?**
  - 웹 서버이자 리버스 프록시 서버
  - 클라이언트(브라우저)의 요청을 받아 내부 애플리케이션 서버(Gunicorn)에 전달하고, 응답을 다시 클라이언트에게 돌려주는 중간 역할
  - 정적 파일(CSS, JS, 이미지)은 Nginx가 직접 처리하고, 동적 요청(API, Django 처리)만 Gunicorn으로 넘겨서 성능 향상
  - SSL 인증서(HTTPS) 처리도 Nginx가 담당
  - `proxy_read_timeout` 같은 설정으로 응답 대기 시간을 조절할 수 있음
  - 설정 파일: `/etc/nginx/sites-available/` → symlink로 `sites-enabled/`에 연결

- **EC2 리전이 성능에 미치는 영향**: 서버가 시드니(ap-southeast-2)에 있으면 한국 API(Naver, KOPIS 등)까지 왕복 지연이 커짐. Naver 검색 54초 → 서울 리전으로 이전하면 5~10초 예상

- **Gunicorn worker timeout**: 기본 30초 안에 응답 못하면 워커가 SIGKILL 됨. 스트리밍 응답이나 처리 시간이 긴 요청은 `--timeout` 값을 늘려야 함

---

## 3. 다음 목표

### 인프라
- [ ] EC2 서울 리전(ap-northeast-2)으로 이전 (Naver API 응답 속도 개선)
- [ ] GitHub 연동으로 배포 자동화 (`git pull` + `systemctl restart`)
- [ ] 네이버 지도 API 키에 `japantourtravel.com` 도메인 등록 (지도 표시 오류 해결)

### 기능
- [ ] 플랜 계정별 저장 기능 정상 동작 재확인 (로그인 여부별 저장·불러오기)
- [ ] 부산 K-pop 선택 시 KOPIS 공연정보 없는 원인 분석 및 처리 개선
