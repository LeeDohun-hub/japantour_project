# 업무 보고 — 2026-05-22

## 개요

위저드 **🗺 관광** 단계 개선: 일정·Places 검색 **지역 불일치** 수정, **희망 관광 에리어(`regions`) 우선** 로직 반영, **시·군·구** 칩 선택 UI 도입 및 목록 정리.

---

## 1. 현상 (사용자 이슈)

| 문제 | 예시 |
|------|------|
| 숙박은 고양·일산인데 식당·관광이 인천 위주 | 1일차 체크인 후 **닭큐멘터리치킨(미추홀)** 등 인천 치킨이 표시됨 |
| 광역 선택과 일정 혼재 | 경기·인천 선택 시 일산호수공원·원마운트와 인천 치킨이 같은 날·엇갈린 날에 섞임 |
| 도시·구 UI 미동작 | 「その他 任意」 텍스트만 보이고 **시·군·구 칩**이 안 보임 |

### 원인 요약

1. **Places 검색**: 숙박 주소·인천·경기 맛집을 한꺼번에 수집 → LLM이 일별 에리어를 무시하고 인천 후보를 골라 붙임.
2. **우선순위**: 숙박 근교를 검색·프롬프트에서 먼저 두던 로직 → 사용자는 **🗺 관광에서 고른 에리어** 우선을 원함.
3. **프론트**: `region-areas.js`를 HTML에서 불러왔으나 **Django URL 미등록** → `REGION_CITY_OPTIONS`가 비어 칩 패널이 빈 상태.

---

## 2. 백엔드 수정 (`src/chain/router.py`)

### 2.1 희망 관광 에리어 우선

- `_tourism_search_areas()` — `regions` + `regionCities`에서 검색·일정용 에리어 추출.
- `_prioritize_itinerary_areas()` — **관광 에리어 먼저**, 숙박 근교는 **맨 뒤**에만 보조.
- `_sort_food_queries_by_tourism_priority()` — Places 맛집 쿼리도 동일 우선순위.
- `_detect_itinerary_areas()` — 숙박 주소를 1차 키워드 스캔에서 제외, 관광 칩·중점 도시 우선.

### 2.2 일별·식사 후보 분리

- `_fmt_itinerary_daily_area_binding()` — `regions` **선택 순서**로 2일차 이후 배정 (인천일 / 경기일 / 서울일).
- `_fmt_itinerary_food_by_day_zones()` — 【인천·희망 에리어】【경기·희망 에리어】 등 섹션 분리 (숙박 전용 버킷 폐지).
- 1일차: 입국·이동·체크인·휴식 (외식 업소명 원칙적으로 없음).

### 2.3 프롬프트 (일본어 UI용 지침)

- 「🗺 희망 에리어 우선」 — 숙박지만으로 관광·식사를 정하지 않음.
- 「일별 에리어」 — 식사 후보 섹션을 해당 날짜에만 사용.

### 2.4 기타

- `_ITINERARY_AREAS` — 인천·랜더스·일산·KINTEX 등 키워드 추가.
- 경기 `chicken` 검색: `고양시` / `수원시` (일산·덕양구 개별 검색은 통합·축소).

---

## 3. 프론트엔드 수정

### 3.1 관광 단계 UI (`frontend/home.html`, `wizard.js`, `styles.css`)

| 단계 | 내용 |
|------|------|
| ① 광역 에리어 | 서울 / 경기 / 인천 / 강원 / 충청 / 전라 / 경상 / 제주 (기존 칩) |
| ② 시·군·구 | ① 선택 후, 광역별 **시군구 칩** 표시 (1개 이상 필수) |
| 그 외 | `<details>` 접기 (목록에 없는 곳만 자유 입력) |

- `setupRegionCityPicker()`, `syncRegionCityPanels()`, `buildRegionCitiesString()`
- 검증: 광역 1개 이상 + **시군구 칩 1개 이상** (자유 입력만으로는 통과 불가)
- 플랜 프롬프트에 `【방문 희망 시·군·구】` 라벨 출력 (UI 문구는 일본어)

### 3.2 `frontend/region-areas.js` (신규·갱신)

**전국 8광역 × 시·군·구** 정의 (`label`: 일본어 표시 / `query`: 한국 행정구역명·Places 검색용).

#### 인천 (사용자 지정 반영)

| 제거 | 유지 |
|------|------|
| 랜더스필드 | 미추홀구 (문학·弥楚忽) |
| 인천 차이나타운 | 연수구 (송도·松島) |
| | 부평구, 중구 (월미도), 남동구, 서구 |

#### 경기도

- **고양시** 하나로 통합 (일산·덕양·화정 분리 제거)
- 수원시, 성남시, 용인시, 부천시, 안양시, 파주시, 가평군, 화성시, 시흥시, 남양주시, 평택시 등

#### 그 외 광역

- 서울: 종로구·중구·마포구·강남구 등 (구 단위)
- 강원 / 충청 / 전라 / 경상 / 제주: 관광 수요가 큰 주요 시·군·구

※ 경기도 전체 시군구(40개 이상)는 UI 과다로 칩 미노출. `wizard.js`의 `ADDR_DATA`에는 숙박 주소용으로 전량 유지.

### 3.3 Django 정적 파일 제공 (버그 수정)

| 파일 | 변경 |
|------|------|
| `backend/tour_api/views.py` | `serve_region_areas_js()` 추가 |
| `backend/tour_api/frontend_urls.py` | `path("region-areas.js", ...)` |

`home.html` 로드 순서: `region-areas.js` → `wizard.js`

---

## 4. 변경 파일 목록

| 경로 | 역할 |
|------|------|
| `src/chain/router.py` | 희망 에리어 우선·일별 컨텍스트·프롬프트 |
| `frontend/region-areas.js` | 시·군·구 마스터 데이터 |
| `frontend/home.html` | 관광 단계 UI |
| `frontend/wizard.js` | 칩 연동·검증·프롬프트 조립 |
| `frontend/styles.css` | 지역 그룹·안내 문구 스타일 |
| `backend/tour_api/views.py` | JS 서빙 |
| `backend/tour_api/frontend_urls.py` | URL 라우팅 |

---

## 5. 동작 확인 절차

1. `docker start japantour-pg` (필요 시)
2. `python backend\manage.py runserver 127.0.0.1:8000`
3. 브라우저 **강력 새로고침** (Ctrl+Shift+R)
4. 개발자 도구 → Network: `region-areas.js` **200** 확인
5. ⑤ 관광: 경기 + 인천 선택 → **고양시·수원시·미추홀구·연수구** 등 칩 표시 확인
6. 플랜 재생성 → 1일차는 이동·휴식, 2일차 이후는 희망 에리어 순 식사·관광

---

## 6. 잔여 과제·메모

| 항목 | 내용 |
|------|------|
| 스포츠 관전 | 랜더스필드는 **시·군·구 목록에서 제거**. 관전일은 `regionCities` 자유 입력·스포츠 칩·경기 일정 API로 별도 처리 가능 |
| 경기 전체 시군구 | 필요 시 `region-areas.js`에 단계적 추가 (안산·의정부 등) |
| 랜더스 | `router._profile_has_landers_focus()` — `regionCities`에 「랜더스」 등이 있으면 인천일 주석 부여 (칩 삭제 후에도 자유 입력으로 가능) |

---

## 7. 설계 의도

- 숙박(고양) ≠ 관광 중심(인천·경기)을 **동시에 선택**하는 일정이 많음 → **관광 칩이 일정의 주축**, 숙박은 이동·체크인용.
- UI 본체는 **광역 + 시군구 칩**. 자유 입력은 보조.

**문제 발생 시**: `region-areas.js` 404, 칩 미표시, 플랜 지역 혼재 → 본 보고서 **3.3·5절** 재확인.

# 2026-05-22 작업 리포트

## 개요

japantour_project 전반에 걸쳐 보안 취약점 보완 및 AI 정확도 개선 작업을 수행했습니다.  
세 가지 주제로 진행했습니다: 프로젝트 취약점 분석 → 보안 보완 → AI 정확도 개선.

---

## 1. 프로젝트 취약점 분석 (askmode)

코드 전반을 검토해 다음 항목을 발견했습니다.

### 🔴 보안 (즉시 보완 권장)
| 항목 | 위치 | 내용 |
|------|------|------|
| `SECRET_KEY` insecure fallback | `settings.py:18` | 환경변수 미설정 시 하드코딩 문자열로 silently fallback |
| `DEBUG=true` 기본값 | `settings.py:23` | 환경변수 미설정 시 DEBUG 모드로 기동 |
| `AUTH_PASSWORD_VALIDATORS = []` | `settings.py:49` | 비밀번호 강도 검증 없음 |
| `@csrf_exempt` 5개 | `views.py` | api_chat, api_register, api_login, api_logout, api_places_enrich |
| Rate Limiting 없음 | `views.py` 전반 | 사용자당 API 호출 횟수 제한 없음 → OpenAI 비용 폭증 가능 |

### 🟠 아키텍처
| 항목 | 내용 |
|------|------|
| `router.py` God Object (3373줄) | 분류·RAG·Places·항공·스포츠·일정 생성이 하나의 파일에 집중 |
| `rag_chain.py` 레거시 껍데기 | `retrieve_context()`가 항상 `_NO_KB_CONTEXT` 반환, 실제 RAG는 router.py에서만 동작 |
| `_rag_cache` 전역 변수 | 멀티프로세스 환경에서 worker별 캐시 불일치 |

### 🟡 기능 빈틈
| 항목 | 내용 |
|------|------|
| 위저드 상태 미저장 | `wizardData` 객체가 새로고침 시 초기화, localStorage 저장 없음 |
| 진단용 `print()` | `views.py:525` — 프로덕션에서도 stdout에 출력 |
| Rate Limiting 없음 | `api_chat` 남용 시 OpenAI 비용 무제한 발생 가능 |

---

## 2. 보안 보완

### 2-1. CSRF 검토 배경

코덱스 의견: `api_chat`(line 426), `api_register`(line 574), `api_login`(line 644), `api_logout`(line 673)이 `@csrf_exempt` 상태. 세션 쿠키 기반이면 운영에서 찝찝하다는 지적.

→ `SESSION_COOKIE_SAMESITE = "Lax"`만으로는 "defense in depth"가 부족하므로 CSRF 토큰도 추가.

### 2-2. 변경 파일 및 내용

#### `backend/config/settings.py`
```
DEBUG 기본값          "true" → "false"
SECRET_KEY            DEBUG=False 시 미설정이면 ValueError 발생 (배포 실수 방지)
AUTH_PASSWORD_VALIDATORS  [] → Django 4종 기본 검증기 (8자 이상, 숫자만 금지 등)
CSRF_TRUSTED_ORIGINS  환경변수 CSRF_TRUSTED_ORIGINS 로 주입 지원
프로덕션 보안 헤더     DEBUG=False 시 SECURE_HSTS_*, SESSION_COOKIE_SECURE,
                      CSRF_COOKIE_SECURE, X_FRAME_OPTIONS, SECURE_CONTENT_TYPE_NOSNIFF
```

#### `backend/tour_api/views.py`
```
@csrf_exempt 5개 전부 제거  (api_chat, api_register, api_login, api_logout, api_places_enrich)
@ensure_csrf_cookie 추가    api_me — 페이지 진입 시 csrftoken 쿠키 자동 발급
_check_chat_rate() 추가     분당 30회·일 300회 제한 (IP/유저 기반, 환경변수 조정 가능)
                             CHAT_RATE_PER_MIN, CHAT_RATE_PER_DAY
print([DIAG]...) 제거       logger.debug() 로 교체
```

#### `frontend/auth.js`, `app.js`, `wizard.js`
```
getCsrfToken() 헬퍼 추가    쿠키에서 csrftoken 읽기
POST fetch 전체 수정 (8곳)  "X-CSRFToken": getCsrfToken() 헤더 + credentials: "same-origin"
  - auth.js : handleLogin, handleRegister, handleLogout
  - app.js  : /api/chat/
  - wizard.js : login, register, logout, /api/chat/, /api/places/enrich/
```

#### CSRF 방어 흐름
```
1. 페이지 로드 → api_me 호출 → Django가 csrftoken 쿠키 발급
2. POST 요청 시 → JS가 쿠키값을 X-CSRFToken 헤더로 전송
3. CsrfViewMiddleware → 쿠키 vs 헤더 값 비교 검증
SameSite=Lax + CSRF 토큰 = 이중 방어
```

---

## 3. AI 정확도 개선

### 3-1. 개선 항목 도출

기존 문제:
- `build_chunk_text()`가 `question_ja + answer_ja`만 사용 → 한국어 쿼리 검색 시 cross-lingual 매칭 불량
- 가게 운영 세부 정보(영업시간·원산지·메뉴가격) 레코드가 검색 결과를 오염시킴
  - 초기에 에리어 기준 필터를 제안했으나, "문경도 여행지가 될 수 있다"는 지적을 받고 **Q&A 타입 기준 필터**로 방향 수정
- `RAG_TOP_K = 5` → 멀티 에리어 병합 시 에리어당 2건으로 부족
- 벡터 검색만 사용, BM25 보완 없음
- 일정 생성(itinerary)에 `gpt-4.1-mini` 사용 → 복잡한 공간 추론 규칙 준수 미흡

### 3-2. 변경 파일 및 내용

#### `requirements.txt`
```
rank-bm25>=0.2.2  추가 (이미 설치됨 확인)
```

#### `src/chain/vector_store.py`

**① 저품질 레코드 필터 `_is_low_value_record()`**
```python
# 감지 패턴 (7종)
"この店の...営業時間/時まで/時から"
"の営業時間は何時から何時"
"この店の...産地/生産/ブラジル/国内産"
"この店の...主なメニュー/メニューは何/価格/ウォン"
"この店の...電話/予約/席数/座席/駐車/テイクアウト/配達"
한국어 동등 패턴 2종

적용 위치: FAISS 빌드, BM25 빌드, pgvector 검색 결과 후처리
```

**② `build_chunk_text()` 개선**
```python
# 이전: question_ja + answer_ja 만
# 이후: question_ja + answer_ja + question_ko + answer_ko + category + area
# 효과: 한국어 쿼리로 검색해도 cross-lingual 매칭 가능
```

**③ CJK bigram 토크나이저 `_tokenize_cjk()`**
```python
# 한국어·일본어 문자: 2-gram 분해
# 그 외: 소문자 공백 분할
# "명동 맛집" → ["명동", "동 ", " 맛", "맛집"]
```

**④ `BM25Index` 클래스**
```python
# rank_bm25.BM25Okapi 기반
# 빌드 시 노이즈 레코드 자동 제외
# 카테고리·에리어 필터 지원
# rank_bm25 미설치 시 graceful fallback
```

**⑤ `_rrf_merge()` — Reciprocal Rank Fusion**
```python
# score_rrf(rank) = 1 / (60 + rank)
# 두 랭킹 리스트의 RRF 점수 합산 → 최종 top-k 반환
```

**⑥ `HybridVectorStore` 클래스**
```python
# 기존 FAISS/pgvector를 래핑
# 벡터 top_k×3 + BM25 top_k×3 → RRF → top_k 반환
# get_vector_store() 가 항상 HybridVectorStore 반환
# BM25는 서버 기동 시 JSONL에서 자동 빌드 (별도 작업 불필요)
```

#### `src/chain/router.py`
```
RAG_TOP_K        5 → 8
                 (멀티 에리어 병합 시 에리어당 2건 → 4건으로 개선)

ITINERARY_MODEL  "gpt-4.1" 추가
                 itinerary 카테고리만 gpt-4.1 full 모델 사용
                 단순 Q&A는 기존 gpt-4.1-mini 유지
                 환경변수 ITINERARY_MODEL 로 오버라이드 가능

LLM 호출 분기    _model = ITINERARY_MODEL if category == "itinerary" else ANSWER_MODEL
```

### 3-3. 테스트 결과

```
PASS: noise filter          (저품질 패턴 감지 정확)
PASS: build_chunk_text      (한국어 + 메타데이터 포함 확인)
PASS: tokenize_cjk          (CJK bigram 동작 확인)
PASS: BM25Index             (노이즈 레코드 1건 자동 제거 확인)
PASS: RRF merge             (두 리스트 → ['B','A','D'] 올바른 순서)
PASS: router constants      (RAG_TOP_K=8, ITINERARY_MODEL=gpt-4.1)
```

### 3-4. FAISS 재인덱싱 안내

`build_chunk_text()` 개선과 노이즈 필터가 FAISS에 반영되려면 아래 명령으로 인덱스를 재빌드해야 합니다.  
BM25는 서버 기동 시 JSONL에서 자동 빌드되므로 별도 작업 불필요합니다.

```bash
python -m src.chain.vector_store --build --force
```

---

## 4. 미완료 / 향후 과제

| 항목 | 내용 | 우선순위 |
|------|------|---------|
| 위저드 상태 localStorage 저장 | 새로고침 시 입력 내용 소실 | 중 |
| `rag_chain.py` 레거시 정리 | 실제로 사용되지 않는 `retrieve_context()` | 낮음 |
| 평가 데이터셋 ground_truth 추가 | 현재 50개 케이스에 expected answer 없음 → Ragas 지표 측정 불가 | 중 |
| 오분류 로그 DB 적재 | 실서비스 오분류 패턴을 few-shot에 반영하는 피드백 루프 | 높음 |
| HyDE 검색 | 가상 답변으로 벡터 검색 → 품질 향상 기대, 지연 시간 트레이드오프 | 낮음 |
| `router.py` 모듈 분리 | 3373줄 → 카테고리별 핸들러 분리 | 낮음 |

---

## 5. 변경 파일 목록

| 파일 | 변경 내용 |
|------|---------|
| `backend/config/settings.py` | DEBUG 기본값, SECRET_KEY 안전화, 비밀번호 검증기, 프로덕션 보안 헤더, CSRF_TRUSTED_ORIGINS |
| `backend/tour_api/views.py` | csrf_exempt 5개 제거, ensure_csrf_cookie, rate limit, print 제거 |
| `frontend/auth.js` | getCsrfToken 헬퍼, POST 헤더 추가 |
| `frontend/app.js` | getCsrfToken 헬퍼, /api/chat/ 헤더 추가 |
| `frontend/wizard.js` | getCsrfToken 헬퍼, login/register/logout/chat/enrich 헤더 추가 |
| `requirements.txt` | rank-bm25>=0.2.2 추가 |
| `src/chain/vector_store.py` | 노이즈 필터, build_chunk_text 개선, BM25Index, HybridVectorStore, RRF |
| `src/chain/router.py` | RAG_TOP_K 8, ITINERARY_MODEL gpt-4.1, 모델 분기 |

---

## 8. Cursor Agent 세션 — 항공·UI·티켓·네비 (이어서)

> 본 절은 동일 일자 **Cursor Agent** 대화에서 진행한 작업을 정리합니다. (§1~7과 별도 스트림)

### 8.1 개요

| 영역 | 요약 |
|------|------|
| 항공 (Step 2) | KAC(GMP·PUS·CJU) odcloud 스케줄 표시 정책, ICN 실시간 분리, 왕복·출발시각 정렬, 일본어 UI |
| 교통 (Step 4) | AREX·地下鉄 칩을 **鉄道・地下鉄** 1칩으로 통합 |
| 인터파크 | 여행 `regions` 기준 공연 필터 (수도권 여행 시 대구·부산 등 제외) |
| 링크 카드 | 이미지 크롤 실패 시 🎫 placeholder **미표시** (텍스트만) |
| 헤더 | **ログアウト** ↔ **AIチャット** 사이 **プランモード** 버튼 (1~8단계 위저드) |
| 검토 (Ask) | 프로젝트 헛점·취약점 목록 검증, **Django `:8000` vs Streamlit** 역할 정리 |

---

### 8.2 항공편 (백엔드·프론트)

#### 정책

| 공항 | 데이터 소스 | 비고 |
|------|-------------|------|
| ICN | `IncheonAirportClient` (실시간) | `INCHEONTRANSPORT_API_KEY` |
| GMP·PUS·CJU | `KoreaAirportsFlightClient` (odcloud / airport.co.kr) | **스케줄·스냅샷** (잔여석·실시간 지연 아님) |

- **노선이 스냅샷에 있으면** → 운항기간(開始·終了) 밖이어도 **요일 매칭 후 표시** (기간 만료 경고 제거 요청 반영).
- **노선이 없으면** → 0편 (ICN 경유 등 **대체 노선 미표시**).
- PUS→HND 등 미운항 노선: “ICN 경유 참고” 메시지·대체 편 **제거**.

#### 프론트 (`frontend/wizard.js`, `home.html`)

- **往復 / 片道** 토글, `flightTo` 변경 시 **到着便·帰国便** 모두 재조회.
- `fetchReturnFlightList()` — `dep=도착지`, `arr=출발지`로 `/api/flights/` 재호출 (귀국편 지원).
- 항공 UI 문구 **일본어** (조회 중·에러·0편 안내 등).
- `_sortFlightsByDepTime()` — codeshare 병합 후에도 **출발 시각 오름차순** 유지.
- `backend/tour_api/views.py` — `_merge_codeshares` 후 `dep_scheduled` 정렬.

#### 문서·스크립트

- `docs/01-개발환경.md` — ICN vs KAC, `KAC_FLIGHT_SOURCE` 표 추가.
- `scripts/probe_kac_flights.py` — odcloud·airport.co.kr 프로브.

---

### 8.3 교통 칩 (Step 4)

- AREX·地下鉄을 분리 칩이 아닌 **単一 `rail` 칩** (`鉄道・地下鉄（AREX・広域鉄道）`).
- `TRANSPORT_RAIL_BY_AIRPORT` — ICN / GMP·PUS·CJU별 프롬프트·칩 문구.
- `home.html`에서 지하철 단독 칩 제거 (하위호환 `arex`/`subway` 값은 `wizard.js` 매핑 유지).

---

### 8.4 인터파크 공연 (`src/api/ticket_platform_events_client.py`)

- `_trip_active_region_keys()`, `_event_matches_trip_region()` — 위저드 `regions`·`regionCities` 기준.
- 수도권·경기 중심 일정: **대구·광주·부산 등 비관련 지역 공연 제외**.
- 제주만 선택 시: 제주 관련 키워드 **엄격 필터**.
- 최종 목록은 지역 매칭 통과 건만 노출 (검색 보강 후에도 재필터).

---

### 8.5 티켓 링크 프리뷰 (`frontend/link-preview.js`, `styles.css`)

- `preview.image` 없음 → 썸네일 영역 **생략** (`ticket-preview-card--no-thumb`).
- 이미지 URL 있으나 로드 실패 → `onerror`로 thumb 제거 후 동일 클래스.
- 로딩 스켈레톤·에러 fallback에서 **🎫 그라데이션 박스 제거**.

---

### 8.6 헤더 — プランモード (`frontend/home.html`, `wizard.js`)

- 위치: `navUserLabel` · `ログアウト` · **`プランモード`** · `💬 AIチャット`.
- `/` 에서 클릭 시 전체 리로드 없이 `goToStep(1)` (8단계 플랜 화면에서도 1단계로 복귀).
- `aria-current="page"`, `title="1〜8ステップで旅行プランを作成"`.

※ `chat.html` 상단에는 동일 네비 미적용 (사이드바 `← ホームに戻る` 유지).

---

### 8.7 Ask 모드 — 검토·합의 사항 (코드 미반영 포함)

| 주제 | 결론 |
|------|------|
| 취약점 목록 (Claude 분석) | DEBUG/SECRET_KEY·Rate limit·위저드 미저장·router God object 등 **대체로 타당** |
| 항공 9번 보정 | GMP/PUS/CJU는 **KAC API·odcloud** 동작 중; `_AIRPORT_GEO`는 Places용. **귀국편**은 위저드에서 별도 fetch 지원 |
| `input_validator` | 구조는 있으나 **`api_chat` 미연결** — “잘 됨”과 “적용됨” 구분 필요 |
| 실행 스택 | **플랜·채팅·OAuth = Django `runserver :8000`** / `streamlit run app_japan_tour.py` 는 **별도 레거시 UI** |

---

### 8.8 변경·관련 파일 (Agent 세션)

| 경로 | 역할 |
|------|------|
| `src/api/aviation_client.py` | ICN/KAC 라우팅, ICN 대체 노선 제거 |
| `src/api/korea_airports_flight_client.py` | odcloud·기간/요일 필터, `KAC_FLIGHT_SOURCE` |
| `backend/tour_api/views.py` | 항공편 정렬 |
| `frontend/wizard.js` | 왕복·항공 JP·정렬·교통·プランモード·CSRF(기존 §2 연동) |
| `frontend/home.html` | 왕복 칩·교통·`btnNavPlanMode` |
| `frontend/link-preview.js` | 티켓 카드 썸네일 정책 |
| `frontend/styles.css` | `nav-plan-mode`, `ticket-preview-card--no-thumb` |
| `src/api/ticket_platform_events_client.py` | 여행 지역별 공연 필터 |
| `docs/01-개발환경.md` | KAC·ICN 환경 변수 |
| `scripts/probe_kac_flights.py` | KAC API 프로브 |

---

### 8.9 확인 절차 (Agent 세션)

1. `python backend\manage.py runserver 127.0.0.1:8000`
2. **②フライト**: GMP↔HND 왕복 — 到着·帰国 목록·時刻順·日本語 라벨.
3. **④交通**: 鉄道 칩 1개, ICN/GMP 선택 시 안내 문구 차이.
4. **⑧プラン**: 인터파크 카드 — 이미지 없을 때 좌측 🎫 없음.
5. 헤더: **プランモード** 클릭 → Step 1 복귀.

---

### 8.10 잔여 (Agent 세션에서 논의만)

| 항목 | 비고 |
|------|------|
| `chat.html` 공통 top-nav | プランモード·ログアウト와 대칭 UI (미구현) |
| 위저드 `localStorage` | §4·§7과 동일 — 새로고침 시 `wizardData` 소실 |
| 플랜 = `/api/chat/` 단일 호출 | LLM·외부 API 비용·환각 — 별도 plan API는 미착수 |
| FAISS 재인덱싱 | §3-4 명령 — BM25/청크 개선 반영 시 필요 |
