# 2026-05-27 업무 일지

## 1. 업무 내용

**여행 플랜 장소 정확도 개선**
- 일정 본문이 "명동 쇼핑", "카페 순회", "전통 잡화 쇼핑"처럼 추상적으로 출력되는 문제를 줄이도록 프롬프트 규칙 강화
- 관광지, 카페, 쇼핑, 식사 항목은 가능한 한 실제 장소명과 `google_maps_uri`를 함께 출력하도록 가드레일 추가
- `center` 미초기화로 itinerary Places 수집이 실패하던 문제 수정
- 호텔 검색과 일반 장소 검색을 `type=hotel` / `type=general`로 분리하여 지도 보강 시 호텔 검색 필터가 섞이지 않도록 개선

**Google Places 카드 UI 보강**
- 장소 카드에 외관 사진 라벨, 별점, 주소, 지도, 경로 외에 "왜 추천하는지"를 설명하는 짧은 가이드 문장 추가
- 장소명 텍스트와 카드 내부 장소명이 중복 표시되는 문제를 제거
- LLM이 지도 URL을 누락하거나 추상 장소명을 남긴 경우에도, 프론트에서 명동거리, 경복궁, 북촌한옥마을, 쌈지길, 인사동길 등 실제 장소 검색어로 보강하도록 처리
- 대구 카페 순회, 김광석거리 카페, 대구간송미술관 주변 카페 등 지역별 카페 보강 키워드 추가

**식사/카페 추천 품질 개선**
- 점심과 저녁은 후보가 있으면 각각 실제 식당명을 사용하도록 규칙 강화
- "점명은 기재하지 않습니다"로 빠지는 카페 순회 문구를 금지하고, 카페 후보가 있으면 실제 카페명과 위치정보를 붙이도록 개선
- 식당 카드에도 간단한 메뉴/추천 포인트 설명이 표시되도록 보강

**인터파크 행사 위치 보강**
- 인터파크 공연/전시 카드에 `venue`가 있을 경우 Google Places 검색으로 회장 위치를 보강
- 티켓 카드 아래에 회장 위치 카드가 함께 표시될 수 있도록 프론트 렌더링 수정
- 예: 대구간송미술관 전시처럼 장소명이 특정 가능한 행사는 티켓 정보와 지도/경로 정보를 함께 보여줄 수 있게 개선

**검증**
- `node --check frontend/wizard.js`
- `node --check frontend/plan-map.js`
- `node --check frontend/link-preview.js`
- `uv run python -m compileall src/chain/router.py backend/tour_api/views.py`

## 2. 배운 것, 이해한 것

- LLM 프롬프트만으로는 추상적인 일정 표현을 완전히 막기 어렵고, 프론트 후처리에서도 실제 장소 카드로 보강하는 안전장치가 필요하다.
- Google Places 카드가 있어도 "왜 이 장소를 추천하는지"가 없으면 여행 가이드로서 맥락이 부족하다. 장소 데이터와 짧은 설명이 함께 있어야 사용자가 판단하기 쉽다.
- 장소명과 카드 UI가 동시에 노출되면 정보가 중복되어 읽기 불편하다. 카드 안에서 장소명이 이미 표시되는 경우 본문 장소명 라인은 숨기는 편이 낫다.
- 인터파크 행사 데이터는 티켓 정보만으로 끝내지 말고, `venue`를 Places로 연결하면 실제 이동 가능한 여행 일정 요소가 된다.
- 호텔 검색 API와 일반 장소 검색 API가 같은 기본 필터를 공유하면 관광지/카페 보강에서 오탐이 생길 수 있으므로 검색 목적을 명시적으로 분리해야 한다.

## 3. 내일 목표

- [ ] 서울, 대전, 대구 샘플 일정 재생성 후 추상 문구가 실제 장소 카드로 치환되는지 확인
- [ ] 카페 순회 선택 시 체인점 위주가 아닌 지역성 있는 카페 후보가 충분히 나오는지 점검
- [ ] 인터파크 행사 카드 아래 회장 위치 카드가 정확한 Google Places 결과로 붙는지 확인
- [ ] 식당 카드의 짧은 가이드 문장이 실제 메뉴와 어긋나는 케이스가 없는지 샘플링
- [ ] 현재 작업트리 변경분을 기능 단위로 나눠 커밋할지 검토
