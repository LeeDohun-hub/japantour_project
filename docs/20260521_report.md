한 일
Docker Postgres (pgvector 포함) 실행

컨테이너 이름: japantour-pg
포트: 5433 (5432는 Windows Postgres가 쓰고 있어서 충돌 방지)
.env 수정

POSTGRES_PORT=5433 → Docker DB로 연결
VECTOR_BACKEND=faiss → 이미 있는 data/vector/tour_knowledge.faiss 로 RAG 사용 (DB 대량 적재 없이 바로 동작)
migrate 성공 (tour_api 포함, vector 확장 OK)

서버 기동 확인 → http://127.0.0.1:8000/api/health/ 200

지금 하실 일
브라우저:

http://127.0.0.1:8000/
http://127.0.0.1:8000/chat/
PC 재부팅 후에는 Docker + DB 먼저:

docker start japantour-pg
cd C:\Workspaces\japantour_project
conda activate japantour_env
python backend\manage.py runserver 127.0.0.1:8000
(지금은 백그라운드에서 서버가 떠 있을 수 있습니다. 터미널에서 이미 돌리고 있으면 그대로 쓰시면 됩니다.)

pgvector DB 검색을 쓰고 싶을 때 (나중에)
.env에서:

VECTOR_BACKEND=pgvector
그다음 (OpenAI 임베딩·시간 소요):

python backend\manage.py import_tour_knowledge
지금은 FAISS + jsonl 이미 있어서 채팅 RAG는 faiss 로도 됩니다.

구조 (기억용)
항목	역할
Windows Postgres :5432
vector 확장 없음 → migrate 실패하던 쪽
Docker japantour-pg :5433
pgvector 있음 → 지금 Django가 여기 연결
pip install pgvector
Python용 (이미 OK)
문제 있으면 docker ps 결과나 터미널 에러만 보내 주세요. Agent 모드에서 import_tour_knowledge까지 돌려 달라고 하셔도 됩니다.

