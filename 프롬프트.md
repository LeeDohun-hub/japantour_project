# /chat 말뭉치(K-Culture) 챗봇 — 작업 이어가기 핸드오프

작성: 2026-06-24 (퇴근 전 중간 저장)

---

## 1. 지금까지 한 것 (커밋·푸쉬 완료된 "검증된 부분")

`/chat`을 K-Culture 일본어 말뭉치(`data/processed/tour_knowledge.jsonl`, 37,275건)로
동작하게 만들고, 평가 파이프라인을 정비했다. **아래는 n=24로 검증 후 커밋·푸쉬한 상태.**

**코드 (`src/chain/router.py`, `src/chain/vector_store.py`)**
- area 하드필터 → **소프트필터**: 빈 area 레코드(약 69%)도 검색 후보로 유지 (vector_store)
- RAG 검색어를 **사용자 원문(일본어)+키워드 결합**으로 변경 → 교차언어 미스매치 해소
- `invalid` 거절 직전 **말뭉치 폴백**: top1 `_score ≥ 0.55`면 거절 대신 `general`로 답변
- 비스트리밍 경로 `_reasoning` 미정의 크래시 수정

**평가 (`evaluation/`)**
- `build_chat_corpus_eval.py`, `evaluate_chat_corpus_ragas.py` (+ `--skip-ragas` 옵션)
- RAGAS RunConfig: `timeout=240, max_retries=1, max_workers=2` + 컨텍스트 축소
  (`RAGAS_MAX_CONTEXTS=3`, `RAGAS_CONTEXT_CHAR_LIMIT=600`) → 4지표 산출 가능
- 문서: `evaluation/docs/EVALUATION_README.md`(의약품 잔재 제거), `docs/chat_answerable_questions.md`,
  리포트 `evaluation/reports/chat_corpus_eval_20260624.md`

**검증 결과 (라우팅, n=120 / RAGAS, n=24)**
| 지표 | 값 |
| --- | ---: |
| route_accept_rate | 65% → **100%** |
| route_rag_used_rate | 65% → **100%** |
| retrieval_hit_rate | 0.958 |
| Faithfulness | 0.733 |
| **Answer Relevancy** | **0.593 ← 최약점** |
| Context Precision | 0.896 |
| Context Recall | 0.917 |

---

## 2. 집에서 이어서 할 것 (남은 분량)

### 목표: Answer Relevancy(0.593 → 0.80) 끌어올리기

n=24 베이스라인 대비, AR을 직접 겨냥한 개선안을 검증한다.
(faithfulness/precision/recall은 이미 양호 — AR만 약점)

### 2-1. 미검증 상태로 보류한 "AR 개선안" (router.py에서 되돌려 둠 — 다시 적용 필요)

`src/chain/router.py`의 `[CORE PRINCIPLES]` 5번 바로 아래에 원칙 6을 추가:

```
6. DIRECT & COMPLETE: Open with a sentence that directly and specifically answers the exact question asked, and cover every part of it. Do not drift into tangential facts, and do not append generic closings that do not answer the question (e.g. "choose from the cards below", "feel free to ask anything", "下のカードから選んでください"). Every sentence should serve the user's specific question.
```

> 이유: AR이 낮은 건 답변이 질문에 직답하지 않고 "카드에서 골라보세요" 같은 무관한
> 마무리로 흐르기 때문. 이 원칙은 omission이 아니라 "직접·완결 답변" 유도라 faithfulness를
> 해치지 않으면서 AR을 올리는 방향. (n=3 사전 테스트는 노이즈라 결론 못 냄 → n=24 검증 필요)

### 2-2. 검증 방법 (반드시 n=24 동일 표본 짝비교)

```powershell
# 환경
$env:VECTOR_BACKEND="faiss"; $env:PYTHONIOENCODING="utf-8"; $env:DJANGO_SETTINGS_MODULE="config.settings"
# (Docker 불필요 시 faiss 인덱스 data/vector/tour_knowledge.faiss 사용)

# AR 개선안 적용 후 동일 24건 재측정 (약 80분 소요)
uv run python -m evaluation.scripts.evaluate_chat_corpus_ragas --limit 24 --output evaluation/reports/chat_corpus_ragas_ar24.json
```

판정 기준:
- **채택**: Answer Relevancy ≥ 0.61 (베이스라인 0.593 대비 상승) **그리고** Faithfulness ≥ 0.70 유지
- **롤백**: AR 하락 또는 Faithfulness < 0.70 → 원칙6 제거하고 다른 접근

### 2-3. AR이 원칙6으로도 안 오르면 다음 후보
- 답변 길이/구성 조정(질문 유형별 템플릿), 마무리 문구 자동 제거 후처리
- 평가 측면: 컨텍스트 축소(3/600)가 AR을 왜곡하는지 점검(평가 한정이라 영향 적을 것)
- 표본 확대(30~50건)로 분산 추가 축소

### 2-4. RAGAS 비용 주의
- faithfulness/context_precision는 비싸다(3건 ~10분, 24건 ~80분). 큰 표본은 시간 확보 필요.
- 일상 점검은 `--skip-ragas`(라우팅+검색 지표, API 무료, 수초)로.

---

## 3. 환경 메모
- 벡터 백엔드: 로컬 테스트는 `VECTOR_BACKEND=faiss`(인덱스 `data/vector/tour_knowledge.faiss`,
  이미 임베딩 포함). 운영은 `pgvector`.
- `python`은 Windows Store 스텁이라 동작 안 함 → **`uv run python`** 사용.
- `.env`는 gitignore(키 포함) — 커밋 금지.

---

## 4. 집에서 바로 붙여넣을 프롬프트

```
japantour_project의 /chat 말뭉치 챗봇 평가를 이어서 한다.
현재 n=24 RAGAS 베이스라인: Faithfulness 0.733 / Answer Relevancy 0.593(최약점) /
Context Precision 0.896 / Context Recall 0.917. route_accept_rate 100%.

목표: Answer Relevancy를 0.80까지 끌어올린다.

1) src/chain/router.py [CORE PRINCIPLES] 5번 아래에 "원칙 6 DIRECT & COMPLETE"
   (프롬프트.md 2-1 참고)를 추가한다.
2) 동일 24건으로 RAGAS 재측정한다(evaluate_chat_corpus_ragas.py --limit 24, faiss, ~80분).
3) 베이스라인 대비 짝비교: AR≥0.61 & Faithfulness≥0.70이면 채택, 아니면 롤백 후 다른 접근.
4) 결과를 evaluation/reports/chat_corpus_eval_20260624.md에 기록한다.
효과 검증 없이 n=3 같은 소표본으로 판단하지 말 것(노이즈).
```
