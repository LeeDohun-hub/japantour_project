# 業務報告 — 2026-05-21

## 要約（番号別・短縮）

**§1 業務内容**

1. **ウィザード・プラン UI** — カテゴリ統合、バカンス・旅行スタイル追加、時刻なしタイムライン、別プラン生成、Google 表示名改善。
2. **認証** — Step 1 メール会員登録（OAuth・ゲストは維持）。
3. **航空・地図** — 便一覧の重複除去、`plan-map.js` エラー修正。
4. **食事・Places** — 好みメニューチップ、ウェディング・配達専用除外、好みフィルタ（検証待ち）。
5. **ドキュメント・運用** — 本報告書作成、`auth_user` 削除 SQL 案内。
6. **バグ（未解決）** — 高陽宿泊＋仁川観光＋食事好みがプランに反映されない。
7. **参考** — LLM 誤表記・リロール効果、Maps API 設定、前日残タスク。

**§2 学んだこと**

1. プラン品質は UI→Places→LLM→表示が連動必須。スポット欄は URL リンク店のみ。宿泊と観光はエリア分離検索。航空は API 側 dedupe 必須。

**§3 明日の目標**

1. 高陽＋仁川＋食事バグ修正 → 食事フィルタ E2E 確認 → リロール・表示名確認 → 報告更新。

---

## 1. 業務内容

### 1. ウィザード・プラン UI

・**Step 5 カテゴリ整理** — `やりたいこと` と `韓流の関心` の重複を解消。ドラマ・K-pop・カフェ巡りを Step 5 に統合し、Step 7 の `韓流の関心` ブロックを削除。
・**バカンス** — `やりたいこと` に 🏖 バカンスを追加。下位でプールヴィラ・ペンションの複数選択に対応。
・**旅行スタイル** — Step 7 に 9 種類の複数選択（体験・SNS・自然・必須観光地・癒し・文化・現地の雰囲気・ショッピング・グルメ）を追加し、プロンプト・ルーターに反映。
・**プラン表示** — `[HH:MM〜HH:MM]` を廃止し、日別に **①②③ / 午前・昼食・午後・夕食** のタイムライン UI に変更。その後、丸数字を削除（縦線・ドットのみ）。
・**プラン再生成** — Step 8 に `🔄 別のプランを生成` を追加（`plan_variant_seed`、`avoid_place_names`、Places シャッフル・追加クエリ）。
・**Google ログイン表示** — `google_109…さん` ではなく表示名・「あなた専用の…」の文言に改善（`_user_display_name`、OAuth で `first_name` を保存）。

### 2. 認証

・**ホーム画面メール会員登録** — Step 1 にログイン／新規登録タブ、表示名・メール・パスワード・確認・利用規約同意（`api_register` / `api_login` を拡張）。
・OAuth（Google / LINE）は維持、ゲスト続行も維持。

### 3. 航空・地図

・**航空便一覧の重複バグ** — 同一便・コードシェア（ZE606×N、KE5742 の繰り返し等）の dedupe（`aviation_client`、`views.py`）。
・**`plan-map.js`** — `PlanMapView` undefined エラーを修正。

### 4. 食事・Places 品質

・**食事制限 → 好きな韓国料理メニュー** — チキン・국밥・カフェ等のチップ選択、避けたいメニューは別途選択。
・**食事候補の品質** — ウェディングホール・コンベンション・**配達専用**（`배달전용` 等）を食事推薦から除外（`filter_meal_places`）。
・**好みメニュー未反映への対応（進行中）** — Places キーワードフィルタ、`gukbap` 追加、LLM の「食事候補」／「観光」分離、候補リスト外の店舗禁止、`wizard.js` のスポットカードフィルタ、Places 並列検索の重複コード整理。

### 5. ドキュメント・運用

・`20260521_report.md` の作成・要約形式の整理。
・`auth_user` 全件削除用 SQL の案内（テスト DB 整理用）。

### 6. バグレポート（未解決・明日修正）

> 宿泊を高陽市に設定し、観光地を仁川に設定し、その途中で食べたいメニューを反映してみたが、該当旅行プランに反映できていないように見える。

・チキン・국밥・コーヒーを選択しても、国数店・韓牛・生魚焼き等が `プラン内のスポット` に表示される事例を再現。
・**高陽宿泊 + 仁川観光 + 移動中の食事**が日程・検索・カードの間で一貫しない問題 — コード修正後、E2E 未確認。

### 7. 参考（本日の議論・前日繰越・未完了）

・LLM のハルシネーション（一山湖公園↔一ちばん湖等）、リロールしても似たプラン — reroll・avoid ロジックは一部強化済みだが再検証が必要。
・Google Maps API キー・サービスアカウント設定に関する問い合わせ。
・前日の残タスク：T005 日本語応答、KB / Ragas、response_time、両替所 等。

---

## 2. 学んだこと・理解したこと

### 1. まとめ

・**プラン品質は入力 UI + データパイプライン + LLM + 表示 UI のセット** — チップだけ追加しても Places 検索・プロンプト・リンクカードが連動しないと、ユーザーは「反映されていない」と感じる。
・**`プラン内のスポット` は LLM が本文に入れた Maps URL のある店だけ**が出る。検索結果の全体ではない。
・**料理ジャンルは店名・キーワードで除外できるが限界がある** — `한우마을`、`생선구이`、`Guksujip` はフィルタ可能だが、店名だけでは判別しづらい店は LLM が選ぶ余地が残る。
・**宿泊エリア ≠ 観光エリア**のときは Places・プロンプトを **エリア別に分ける**必要がある。「宿泊近郊の飲食店」だけ検索すると、仁川観光日・移動区間の食事が抜ける。
・**航空 API はコードシェア・ページ統合時に dedupe が必須** — フロントだけでなく API・`aviation_client` 両方で重複除去が必要。
・**ウィザードのカテゴリはユーザーが一度だけ選ぶ設計がよい** — Step 5 / 7 の二重選択は混乱とプロンプトノイズの原因になる。

---

## 3. 明日の目標

### 1. 対応予定

・**バグ修正（最優先）** — 高陽宿泊 + 仁川観光 + 選択メニューがプラン本文・Day 地図・`プラン内のスポット` に反映されるよう、エリア別 Places / プロンプトを追跡・修正。
・**本日の食事フィルタ検証** — サーバー再起動、Ctrl+F5 のあと、チキン・국밥・カフェ等で再生成し、誤ったジャンル・ウェディングホール・配達専用の表示有無を確認。
・**リロール・表示名** — `別のプランを生成` で日程・飲食店が実際に変わるか、Google ログイン表示名は再ログイン後に確認。
・**報告** — 修正結果を本ファイルまたは `20260522_report.md` に反映。

---

_作成: 2026-05-21_
